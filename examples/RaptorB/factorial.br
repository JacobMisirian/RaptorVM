#include stdlib.br

main () {
	putintln(factorial(5));
}

factorial (n) {
	auto counter, total; counter = 1; total = 1;

	while (counter <= n) {
		total = total * counter;
		counter = counter + 1;
	}

	return total;
}
