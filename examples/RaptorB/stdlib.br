main () {
	putintln(7);
	putstrln("Hai");
	putcharln('r');
	putintln(8);
	putchar(charat("hack", 5));
	strlen("Hack");
}

strlen (str) {
	auto c;
	c = 0;
	while (charat(str, c) != 0) {
		c = c + 1;
	}
	putintln(c);
}

putstrln (str) {
	putstr(str);
	putchar(10);
}

putcharln (ch) {
	putchar(ch);
	putchar(10);
}

putintln (int) {
	putint(int);
	putchar(10);
}
